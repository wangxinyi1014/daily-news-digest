# 📰 AI 早报 · 微信推送部署指南（傻瓜版）

本项目每天自动抓取 **20 个全球媒体源**（BBC / 路透社 / 卫报 / 美联社 / FT / CNBC / 彭博 / 麻省理工科技评论 / Hacker News / TechCrunch / 36氪 等），
由 **DeepSeek AI** 写成深度早报，通过 **Server酱** 推送到你的**微信**。
全程跑在 GitHub Actions 上，**免费、零服务器**。

---

## 第 0 步：准备 3 样东西（共约 10 分钟）

| # | 你需要 | 去哪弄 | 花费 |
|---|--------|--------|------|
| 1 | GitHub 账号 | github.com 注册 | 免费 |
| 2 | DeepSeek API Key | platform.deepseek.com 注册 → 充值 | ¥10 能用几个月 |
| 3 | Server酱 SendKey | sct.ftqq.com → 微信扫码登录 → 「SendKey」 | 免费 |

> 💡 三个都是"注册一下就能拿到"，不需要任何审核。

---

## 第 1 步：把项目复制到你的 GitHub（Fork）

1. 打开本项目 GitHub 页面：`https://github.com/<你的用户名>/daily-news-digest`
2. 点右上角 **Fork** 按钮 → 等几秒，你的账号下就有了一个副本
3. 进入你 Fork 后的仓库页面

## 第 2 步：填入 3 个密钥（Secrets）

在你 Fork 的仓库里：

1. 点 **Settings**（设置）
2. 左侧菜单选 **Secrets and variables** → **Actions**
3. 点绿色按钮 **New repository secret**，逐个添加：

| Name（名字） | Value（值） |
|--------------|-------------|
| `DEEPSEEK_API_KEY` | 你的 DeepSeek key（`sk-` 开头） |
| `SERVERCHAN_SENDKEY` | 你的 Server酱 SendKey（`SCT` 开头） |
| `DIGEST_LANG` | `zh`（中文）/ `en`（英文）/ `bilingual`（中英双语） |

> 想推给多个微信号？SendKey 用英文逗号分隔：`SCTa,SCTb`

## 第 3 步：手动测试一次（1 分钟）

1. 点仓库上方 **Actions** 标签
2. 左侧选 **📰 Daily News Digest**
3. 点右侧 **Run workflow** → 绿色按钮确认
4. 等 2~4 分钟，**微信应该能收到一条早报**
5. 如果失败，点进这次运行看红色日志，把报错发给我

## 第 4 步：自动运行（以后什么都不用管）

我已经把定时调度改成了 **GitHub 自带 cron**（无需外部服务）：

- 🕗 **每天 08:00 北京时间**：晨报
- 🕗 **每天 20:00 北京时间**：晚报

想改时间？编辑仓库里的 `.github/workflows/daily-digest.yml`，改 `cron` 那两行即可（注意 cron 是 **UTC 时区**：北京时间 = UTC + 8）。

---

## 自定义你的早报

所有设置都在 `digest/config.py` 一个文件里改：

- **加新闻源**：`RSS_FEEDS` 列表里加一行（任何 RSS 链接都行）
- **调权重**：`HIGH_SIGNAL_KEYWORDS`（重要词 +3 分）、`MEDIUM_SIGNAL_KEYWORDS`（+1 分）、`LOW_VALUE_KEYWORDS`（-2 分）
- **条数**：`FINAL_PICK`（每天报几条，默认 13）、`CATEGORY_QUOTA`（每类几条）
- **AI 风格**：`digest/ai.py` 里的 system prompt 可以整段改成你的口味

## 常见问题

**Q：必须用 DeepSeek 吗？**
A：不是。任何 OpenAI 兼容接口都行（Gemini / Claude / GPT / 通义千问），改 `.env` 里的 `DEEPSEEK_API_KEY` 和 `digest/ai.py` 里的模型名即可。

**Q：Server酱 被墙怎么办？**
A：项目自带 3 次重试 + Telegram 兜底（可选配）。GitHub 海外 runner 偶尔连不上国内 Server酱 是正常现象，一般重试 2 次内能送达。

**Q：想只看国外资讯？**
A：把 `config.py` 里 `RSS_FEEDS` 的 36kr 等中文源删掉即可。

**Q：免费额度够吗？**
A：DeepSeek ¥10 充值按每天 2 报、每报 13 条算，能用好几个月。Server酱 免费。

---

*本指南由 AI 生成，配合 daily-news-digest 项目使用。项目 MIT 协议。*
